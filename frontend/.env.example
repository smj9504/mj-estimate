# React App Environment Variables - DEVELOPMENT
# Copy this file to .env and fill in your actual values

# API URL (backend server)
REACT_APP_API_URL=http://localhost:8000

# Google Sheets Integration
REACT_APP_GOOGLE_SHEETS_WATER_MITIGATION_ID=your_spreadsheet_id_here

# Notes:
# 1. All React environment variables MUST start with REACT_APP_
# 2. Variables are embedded at BUILD TIME (not runtime)
# 3. Never include sensitive backend secrets (they're visible in browser)
