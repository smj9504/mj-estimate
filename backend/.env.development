# Development Environment Settings
ENVIRONMENT=development
DEBUG=true

# Database Settings - Docker PostgreSQL (Default)
DATABASE_TYPE=postgresql
USE_SQLITE=false
DATABASE_URL=postgresql://mjestimate:dev_password_2024@localhost:5433/mjestimate_dev

# Server Settings
HOST=0.0.0.0
PORT=8000

# Security
SECRET_KEY=dev-secret-key-change-in-production

# CORS Origins (JSON array format for pydantic-settings)
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://127.0.0.1:3000","http://127.0.0.1:3001"]

# Logging
LOG_LEVEL=DEBUG

# Note: Supabase is NOT used in development
# For production deployment, use .env.production with Supabase settings