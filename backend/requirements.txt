# FastAPI and dependencies
fastapi==0.115.14
uvicorn[standard]==0.34.3
python-multipart==0.0.6
pydantic==2.9.2  # Updated for better Python 3.12/3.13 compatibility with pre-built wheels
pydantic-settings==2.1.0
email-validator==2.3.0

# Database
# supabase==2.0.3  # Not used - using PostgreSQL directly
sqlalchemy==2.0.23
redis==6.4.0
psycopg2-binary==2.9.10

# Admin Interface
sqladmin==0.16.1

# CORS and security
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
python-dotenv==1.0.0
itsdangerous==2.2.0
slowapi==0.1.9

# File handling
aiofiles==23.2.1
openpyxl==3.1.2
Pillow>=11.0.0  # Updated for inference-sdk compatibility (requires >=11.0)
python-dateutil==2.8.2  # Flexible date parsing for various input formats
opencv-python-headless>=4.8.0  # Perspective transform for document scanning

# AI/ML - Photo Analysis
openai==1.54.0  # OpenAI GPT-4 Vision for room photo analysis
# google-genai removed — using Gemini REST API directly via httpx (saves ~50MB memory)

# Google Drive Integration
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# Google Cloud Storage (RECOMMENDED for production)
google-cloud-storage==2.14.0

# PDF Generation
# playwright==1.40.0  # Optional - only for browser-based PDF (commented out for faster deployment)
weasyprint==68.1
cairosvg>=2.7.0  # SVG to PNG rasterization for PDF rendering (MagicPlan floor plans)
jinja2==3.1.2
pypdf>=5.0.0  # Updated to avoid ARC4 deprecation warning (was 3.17.0)
pdfplumber>=0.11.0
pytesseract>=0.3.10
pdf2image>=1.17.0
pymupdf>=1.24.0
reportlab==4.0.7

# Email (optional, for sending documents)
# python-emails==0.5.15

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
httpx>=0.27.0

# Scheduling
apscheduler==3.10.4
pytz==2024.1  # Timezone support for business hours scheduling

# Database Migration
alembic==1.13.1

# Material Detection APIs (disabled for deployment optimization)
# roboflow==1.1.11
# inference-sdk==0.59.0
# google-cloud-vision==3.5.0

# AI/ML Models for Material Detection (Optional - very large, install separately if needed)
# transformers>=4.30.0  # Hugging Face Transformers for Custom ViT (300MB+)
# torch>=2.0.0  # PyTorch for deep learning models (500MB+)
# pillow>=11.0.0  # Already included above

# Image Processing (disabled - only needed for Material Detection)
# opencv-python==4.10.0.84
# opencv-python-headless==4.8.0.76
# numpy>=2.0.0,<2.3.0  # NumPy 2.x required for inference-sdk

# Redis Caching (async)
aioredis==2.0.1

# Xactimate Helper Tool - AI Pipeline
anthropic>=0.39.0  # Claude API for Vision + LLM matching
pgvector>=0.3.6  # PostgreSQL vector similarity search
numpy>=1.26.0  # Required by pgvector